import React from "react";
import ChartItemComponent from "./ChartItemComponent";

export default function AnalysisChart({ chartRef }: { chartRef: any }) {
  return <ChartItemComponent chartRef={chartRef} />;
}
